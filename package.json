{
  "name": "story-sentinel",
  "version": "1.0.0",
  "description": "StorySentinel - AI-powered IP protection platform using Story Protocol and Yakoa",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "contracts"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../contracts && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "ip-protection",
    "story-protocol",
    "yakoa",
    "blockchain",
    "ai",
    "infringement-detection"
  ],
  "author": "StorySentinel Team",
  "license": "MIT"
}
